export interface CreateDeskType {
  name: string;
  address: string;
}

export interface DeskState {
  id: string;
  name: string;
  address: string;
}

export interface DeskType {
  id: string;
  name: string;
  status: boolean;
}
